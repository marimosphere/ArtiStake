import { ethers } from "hardhat";
import * as chai from "chai";
import { solidity } from "ethereum-waffle";

chai.use(solidity);
const { expect } = chai;

describe("ArtistAaveMaticVault", function () {
  it("User can stake to Artist", async function () {});

  it("Artist can get yield", async function () {});

  it("User can get yield", async function () {});

  it("User can withdraw from Artist", async function () {});
});
